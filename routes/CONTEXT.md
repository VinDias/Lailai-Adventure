# routes/

## Responsabilidade

Endpoints da API REST do Express. Cada arquivo agrupa rotas relacionadas a um domínio específico da aplicação.

---

## Arquivos e Endpoints

### `content.js` — Conteúdo Principal
Rotas públicas e protegidas para consumo de conteúdo.

> **Fase 5, Bloco 1 (Task 2) — "Drafts invisíveis ao público".** Antes deste bloco, um capítulo `draft` dentro de uma série publicada aparecia na busca, na lista de capítulos e no detalhe. Como o portal do ilustrador tem como pilar "nada vai ao ar sem aprovação do Master", as rotas públicas passaram a filtrar por publicado. Regra única em `utils/ownership.js::podeVerRascunho` (admin **ou** dono do canal da série); qualquer outro viewer recebe **404, nunca 403** — 403 confirmaria a existência do rascunho alheio. Superfícies **já seguras antes** e conferidas por teste: `GET /series` (`content.js:143` filtra `isPublished: true`), `GET /agenda` (`content.js:111`), `GET /recommendations` (o service filtra), favoritos (`routes/favorites.js`) e "continuar lendo" (`services/progressService.js:222`).

- `GET /series` — Listagem de séries (filtros por tipo: `hqcine`, `vcine`, `hiqua`); só publicadas
- `GET /series/:id` — Detalhes de uma série. Ganhou `optionalAuth` (`content.js:173`): série `isPublished: false` é **404** para quem não é admin nem dono do canal
- `GET /series/:id/episodes` — Episódios de uma série (`content.js:355`). Série inexistente **ou** rascunho fora do alcance do viewer → `[]` com 200 (contrato antigo preservado, sem confirmar existência). Viewer comum recebe só `status: 'published'`; admin/dono recebem os rascunhos **com** o campo `status` — é disso que a aba Obras do portal depende
- `GET /episodes/:id` — Detalhes de um episódio (`content.js:403`). "Publicado" = `status === 'published'` **E** a série-mãe `isPublished === true`; fora disso, 404 para quem não é admin/dono. **Views e `EngagementEvent` só incrementam dentro do ramo publicado** — admin ou dono revisando o próprio rascunho não infla contador de royalties nem gera telemetria
- `GET /search` — busca; o filtro de episódios ganhou `status: 'published'` (`content.js:48-51`), além do filtro de série publicada que já existia
- `POST /series/:id/vote` — Sistema de votação (like/dislike) — requer autenticação. Fase 4, Bloco 4: dispara `recommendationService.dispararRecalculo(seriesId, 'voto_serie')` SEMPRE (upsert não distingue voto novo de trocado; recalcular de novo com o mesmo voto é idempotente), inclusive no `E11000` de corrida de dois primeiros-votos simultâneos
- `POST /series`, `PUT /series/:id`, `DELETE /series/:id` — CRUD de séries (admin only). Fase 5, Bloco 1 (Task 7): o corpo do `PUT` foi **extraído** para `services/seriesPublishService.js::applySeriesUpdate` — a rota virou um wrapper fino (`content.js:230-252`) e a Fila de Aprovação chama a MESMA função, para gênero obrigatório/tradução/redisparo não divergirem entre os dois caminhos. Fase 4, Bloco 4: POST/PUT aceitam `tags` (validação do schema — 0 ou 5–15, strings não vazias, `models/Series.js`); `ValidationError` do Mongoose (tags inválidas — mas também qualquer outro campo, ex. `releaseDay`) vira **400** em vez de cair no catch genérico (500) nas duas rotas
- `POST /episodes` (admin) — Fase 5, Bloco 2 (Task 8, higiene do Bloco 1): `episode_number` duplicado na MESMA série agora é **400** (`content.js:490-492`, `Episode.exists({seriesId, episode_number})` ANTES do `Episode.create`) — validação NA ROTA, não índice único (séries antigas podem já ter duplicatas de antes desta task; um índice único quebraria a LEITURA delas). Mesma numeração em outra série é permitida. Mesma checagem do lado do portal — ver `portal.js` abaixo
- **CastError → 404/400** (fix round da Task 8 — o revisor achou que a divída "CastError → 500" só tinha sido fechada PARCIALMENTE, sobrando `content.js`): `GET /series/:id` (`content.js:192`), `GET /series/:id/episodes` (`content.js:389`) e `GET /episodes/:id` (`content.js:471`) tratam id malformado via `utils/routeErrors.js::responderCastError` — 404, testado ANÔNIMO e LOGADO (as 3 são `optionalAuth`). `POST /episodes` (`content.js:524`) trata `episode_number` não-numérico (ex. `'abc'`) pelo MESMO helper — o `Episode.exists({seriesId, episode_number})` lança `CastError` com `path: 'episode_number'` (não `_id`), então vira **400** legível (nunca 404 — esta rota não tem `:id` na URL)
- `GET /recommendations?type=hqcine\|vcine\|hiqua` (Fase 4, Bloco 4) — lista de séries publicadas do tipo, ordenada pela recomendação 50/30/20 (`services/recommendationService.buildRecommendations`). `optionalAuth` + identidade anônima (`utils/requestIdentity`, mesmo header `X-Anonymous-Id` do progresso) alimentam a Afinidade do leitor. Qualquer falha do serviço (score ausente, agregação etc.) NUNCA vira 500 direto — degrada para a MESMA query manual do `GET /series` (`order_index` asc, `createdAt` desc); só se o próprio fallback falhar (ex. banco fora do ar) é que responde 500. `type` fora do enum → 400
- Republicação de série (`isPublished` falso→verdadeiro no PUT) também dispara `dispararRecalculo(seriesId, 'capitulo_publicado')` — mesmo gatilho fire-and-forget dos "6 pontos de publicação" do algoritmo (Fase 4, Bloco 4, mesmo conjunto do push do Bloco 2): republicação de série (3º) + `POST /episodes` (1º) + `PUT /episodes/:id` (2º) + `POST /episodes/:id/panels` (4º), todos neste arquivo, mais o webhook de status do Bunny (5º) e a sincronização manual do status Bunny (6º) em `bunnyWebhook.js`. A abertura de episódio (`GET /episodes/:id`, view/read) **NÃO** dispara recálculo síncrono — coberta só pela varredura de 24h (RULING da T5: rota mais quente do backend)

### `admin.js` — Painel Administrativo
Endpoints exclusivos para administradores. Montado em `/api/admin/management`.
- `GET /stats` — Estatísticas do dashboard (usuários, receita, conteúdo)
- `GET /content` — Listagem paginada de todo o conteúdo
- `PUT /reorder` — Reordenação de séries via drag & drop
- `PUT /update-thumbnail/:id` — Troca de capa de série
- `PATCH /episodes/:id/audio` — Atualiza `audioTrack1Url` e/ou `audioTrack2Url` de um episódio

### `adminManagement.js` — Gestão de Usuários Admin
- Modificação de roles e permissões de usuários
- Registro de auditoria de ações administrativas (`AdminLog`)

### `payment.js` — Pagamentos Stripe
- `POST /checkout` — Cria sessão de checkout Stripe para assinatura premium
- `POST /webhook` — Webhook Stripe para confirmar pagamentos e ativar premium. No `checkout.session.completed` (Fase 4, Bloco 3): **antes** do caminho Premium, `if (session.metadata?.tipo === 'super_reader')` delega a `superReaderService.registrarContribuicao(session)` e retorna — não é assinatura, não pode cair na busca por `stripeCustomerId`. Falha na gravação cai no catch geral (500) e o Stripe reenvia (o upsert do serviço absorve o retry). Guard adicionado durante o Bloco 3 (achado ao testar o webhook do Super Reader, corrigido de imediato — bug pré-existente, não exclusivo do Super Reader): sessão sem `customer` (ex. doação avulsa, "send test webhook" do painel Stripe) é ignorada com `warn` em vez de cair em `findOne({stripeCustomerId: customerId})` com `customerId` undefined — esse filtro casaria qualquer usuário sem o campo e promoveria um usuário arbitrário a Premium. Fase 4, Bloco 4: o caminho Super Reader só chega em `registrarContribuicao` depois do guard de `payment_status` (segue só se ausente ou `'paid'` — Pix/boleto com `payment_status` diferente retorna cedo sem gravar); logo depois de gravar, dispara `recommendationService.dispararRecalculo(contribuicao.seriesId, 'super_reader')` fire-and-forget, sem `await` (o webhook do Stripe não pode esperar o recálculo pra responder)

### `superReader.js` — Apoio Direto ao Autor (Fase 4, Bloco 3)
Montado em `/api/superreader`. Rotas finas por cima de `services/superReaderService.js` — toda validação/regra de negócio vive no serviço.
- `POST /create-session` — `verifyToken`; body `{ seriesId, amountCents, currency }`. `userId` SEMPRE vem do token (`req.user.id`), nunca do body. Delega a `criarSessaoDeApoio` (valida `amountCents` inteiro ≥ mínimo, moeda somente `brl` — decisão da revisão final: o relatório de repasse agrega sem separar moeda; multimoeda é dívida —, série existe/publicada/com `channelId`; cria a sessão Stripe `mode: 'payment'`) e devolve `{ url }`. Erros de validação (`err.status`) viram o status certo (400/404); qualquer outro erro vira 500
- `GET /me` — `verifyToken`; devolve `{ superReader: boolean, contribuicoes: [{ seriesTitle, amountCents, currency, createdAt }] }` a partir das próprias `SuperReaderContribution` (mais recente primeiro). `superReader` é derivado (existe ≥ 1 contribuição), sem campo novo em `User`. Nunca devolve `stripeSessionId` nem os campos de share (autor/plataforma); série apagada vira `seriesTitle: null` (populate, mesmo padrão de `routes/favorites.js`)
- `GET /min` — sem auth; devolve `{ minCents }` via `services/superReaderService.js::lerMinimoCents()` (não via `PUBLIC_KEYS` de `routes/settings.js` — aquela rota devolve `Setting.value` cru, sem validar/aplicar o fallback default 500)

### `bunnyWebhook.js` — Integração Bunny.net
Montado em `/api/bunny`.
- `POST /webhook` — Recebe eventos do Bunny Stream (transcodificação concluída/falha) e atualiza `Episode.status` no MongoDB. Fase 4, Bloco 4: quando o status do Bunny publica o episódio (Status 4), dispara `recommendationService.dispararRecalculo(episode.seriesId, 'capitulo_publicado')` — 5º dos "6 pontos de publicação" (mesmo conjunto do push do Bloco 2; os outros ficam em `content.js` — POST/PUT episódio, POST panels, republicação de série — e um 6º aqui mesmo, na sincronização manual do status Bunny)
- `POST /upload` — Cria vídeo na biblioteca Bunny Stream e retorna URL TUS para upload direto
- `POST /upload-video` — Upload de arquivo de vídeo direto para o Bunny Stream via multipart
- `POST /upload-image` (`bunnyWebhook.js:213`) e `POST /upload-image-batch` (`bunnyWebhook.js:283`) — **as duas ÚNICAS rotas de upload abertas ao dono de canal** (Fase 5, Bloco 1, Task 5): `verifyToken` + "admin **OU** dono de canal ativo". Imagem única cai em `lorflux/series/<slug>/covers/`; o lote (até 138 arquivos, `Promise.allSettled`, relatório por arquivo) cai em `lorflux/series/<slug>/panels/`.
  - **O contrato do slug é diferente por perfil** (`resolveUploadSlug`, `bunnyWebhook.js:33`). **Admin**: contrato ANTIGO intacto — `seriesSlug` texto-livre do body, slugificado por `toSlug`. **Dono não-admin**: precisa mandar `seriesId` REAL; o servidor resolve série→canal→`ownerId` (`utils/ownership.js:42`) e **deriva o slug do título da série resolvida** — um `seriesSlug` enviado por não-admin é IGNORADO. Slug de texto livre não dá objeto validável: A escreveria na pasta de B
  - Dois 4xx distintos, de propósito: **403** para quem não é dono de canal nenhum (nada a esconder), **404** para série alheia/inexistente/id malformado (mesmo critério de não confirmar a existência de rascunho alheio)
  - O gate roda **ANTES do multer** — a rejeição de quem não é dono nem gasta banda de upload. Os `await` de Mongo do gate ficam dentro de `try/catch` (falha de infra vira 500 logado, não unhandled rejection)
- `POST /upload`, `POST /upload-video`, `POST /upload-audio` — seguem **admin-only explícito** (`bunnyWebhook.js:141`, `:429`, `:371`). Abrir vídeo/áudio ao dono reabriria o buraco do "vídeo grátis" que o gate de temporada da 5.1 vai cobrar — qualquer mudança nesse sentido está errada por definição enquanto o gate não existir
- `GET /signed-url` (`bunnyWebhook.js:562`) — `optionalAuth`; além do fail-closed de `BUNNY_TOKEN_KEY` que já existia, Fase 5 Bloco 1 (Task 2): só assina vídeo de episódio **publicado em série publicada**; fora disso exige admin/dono do canal (mesmo `podeVerRascunho` das rotas de conteúdo), senão 404. Sem isso, um `bunnyVideoId` vazado daria playback de rascunho

### `ads.js` — Anúncios
- `GET /ads` — Lista anúncios ativos
- `POST /ads` — Cria nova campanha (admin only)
- Controle de ativação e agendamento

### `channels.js` — Canais de Criadores
Montado em `/api/channels`. Fase 5, Bloco 1 (Task 1): o canal deixou de ser criável por qualquer usuário — vincular um canal a um ilustrador é decisão do Master.
- `GET /` — lista canais (`verifyToken` + `requireAdmin`); `.select('name ownerId isActive')` (`channels.js:28`). Sem `?includeInactive=true`, só **ativos** (regressão do shape antigo). **Fase 5 Bloco 2 (Task 8, higiene do Bloco 1):** `?includeInactive=true` (`channels.js:26-27`) devolve TODOS, com `isActive` no shape — a rota já é admin-only, então o parâmetro só existe pra quem já passou por `requireAdmin`. Conserta o buraco do Bloco 1: antes, um canal desativado simplesmente sumia da lista do admin no próximo refetch, levando junto a porta de entrada pras threads arquivadas (`CanaisPanel`)
- `GET /me` — `verifyToken`; canais ativos do usuário logado. **Devolve o documento cru, `followers[]` incluído** (dívida registrada — ver "Dívidas conhecidas" ao final)
- `GET /:id` — **público** com `optionalAuth` (`channels.js:58`). Shape pinado da Fase 5: `followersCount` (número) + `isFollowing` (bool, `false` para anônimo), **sem** o array `followers[]` (`channels.js:66-69`). Canal `isActive: false` → 404 para público/não-admin. **Fase 5 Bloco 2 (Task 8):** admin enxerga o canal mesmo inativo (`channels.js:64` — `!channel.isActive && !isAdmin(req.user)`), necessário pro `CanaisPanel` buscar o detalhe completo (dono populado etc.) de um canal listado via `includeInactive`
- `POST /` — **admin-only** (`channels.js:82`, era `verifyToken` puro). Mudança de comportamento deliberada: sem ela qualquer leitor viraria "ilustrador" pelo critério derivado. O canal nasce com `ownerId: req.user.id` (o próprio admin) — vincular ao ilustrador é o passo seguinte, pelo `ownerEmail` do PUT
- `PUT /:id` — `verifyToken`, com dois ramos (`channels.js:112`). **Admin**: edita qualquer canal (busca por `findById`, sem filtro de dono) e é o ÚNICO que processa `ownerEmail` — resolve e-mail→`User` (lowercase + trim; e-mail inexistente → 404 sem salvar nada), e, se for troca real, **arquiva a thread de mensagens ANTES de trocar `ownerId`** (`channels.js:136`). `ownerEmail` do mesmo dono vigente é no-op (não arquiva). **Não-admin**: escopado a si (`findOne` com `ownerId`) e `ownerEmail` no body → **403 sempre**, nunca ignorado em silêncio (`channels.js:117-119`). Resposta devolve o doc cru com `followers[]`
- `POST /:id/desativar` — admin-only (`channels.js:169`): `isActive: false`. É a porta de saída que destrava a exclusão de conta do dono (LGPD)
- `POST /:id/reativar` — **nova (Fase 5 Bloco 2, Task 8)**, admin-only (`channels.js:197`): `isActive: true`, inverso simétrico de desativar. **NÃO desarquiva nenhuma `MensagemPortal`** (decisão documentada no próprio código, `channels.js:191-196`) — a thread arquivada pertence ao ex-dono (arquivada na transferência de titularidade, ver `PUT /:id` acima); o dono ATUAL já tem a própria thread vigente intacta
- `POST /:id/follow` · `DELETE /:id/follow` — seguir/deixar de seguir (`verifyToken`, `$addToSet`/`$pull`); devolvem `{ success, followers: <contagem> }`. Inalteradas neste bloco; **não checam `isActive`** (é possível seguir um canal desativado, que o `GET /:id` já 404 esconde — cosmético, registrado)
- **CastError → 404** (Fase 5 Bloco 2, Task 8): TODAS as rotas com `:id` acima tratam `id` malformado via `utils/routeErrors.js::responderCastError` — 404 quando `err.path === '_id'` (o ÚNICO `CastError` de topo que estas rotas produzem: vem do `findById`/`findOne`/`findByIdAndUpdate` com o próprio `:id` da URL). **Campo do BODY com tipo incompatível não passa por `responderCastError` aqui** — `name`/`description`/`avatar`/`banner` do `PUT /:id`/`POST /` recebendo um array faz a atribuição direta (`channel.name = name`) + `channel.save()`/`Channel.create()` rodar a validação COMPLETA do documento e lançar **`ValidationError`** (com um `CastError` ANINHADO em `err.errors.<campo>`, nunca um `CastError` de topo). Fix round da Task 8 (achado do revisor: a frase original — "CastError em outro campo vira 400" — estava FALSA aqui; a sonda dava 500, `responderCastError` nunca via um `CastError` de topo pra tratar) adicionou `if (err.name === 'ValidationError') → 400` nos dois catches (`channels.js:98-100` POST, `channels.js:158-160` PUT), mesmo padrão de `routes/content.js` `POST /series`. **Em `adminPortal.js` a mecânica É DIFERENTE** (verdadeiro `CastError` de topo, sem `ValidationError` — ver seção abaixo): lá, `applySeriesUpdate` usa `findByIdAndUpdate(..., {runValidators:true})`, cujo CAST acontece ANTES da validação e lança `CastError` puro pro campo

### `portal.js` — Portal do Ilustrador (Fase 5, Bloco 1)
Montado em `/api/portal` (`server.js:264`). `router.use(verifyToken)` no topo (`portal.js:24`) + `requireCanalDoUsuario` em **toda** rota.
- `requireCanalDoUsuario` (`portal.js:36`) — exige ser `ownerId` de ≥1 canal com `isActive: true`; senão **403** (aqui não há segredo a esconder: a área simplesmente não existe para quem não é ilustrador). Anexa `req.portalChannels` / `req.portalChannelIds`, que as rotas usam sem reconsultar
- Posse do recurso ESPECÍFICO (série/capítulo) é checada por `serieDoDono` (`portal.js:232`) e `episodioDoDono` (`portal.js:245`), que devolvem `null` tanto para inexistente quanto para alheio → a rota responde **404** (nunca 403 — A não distingue "não existe" de "é de B")
- `GET /meu-estudio` (`portal.js:51`) — canais do usuário + contagens: obras, `pendentes` (séries e capítulos com `submittedAt` e ainda não publicados) e `mensagensNaoLidas`
- `GET /resumo?period=YYYY-MM` (`portal.js:101`) — painel de números, pelas MESMAS agregações do relatório admin (`services/royaltyReportService.js`), escopadas aos canais do usuário. **Mês corrente: pontos/views válidas/share %, SEM R$** (decisão de contrato — o pool só é verdade no fechamento); períodos FECHADOS trazem `amount` do `RoyaltyPeriod.breakdown` filtrado ao canal. Super Reader vem sempre por mês. Período inexistente → 404; `period` malformado → 400
- `GET /series` (`portal.js:187`) — lista as próprias séries (rascunho/em análise/publicada), `createdAt` desc, `select` de 9 campos leves SEM `translations` — mas **COM `tags`** desde a Task 6 do Bloco 2 (`portal.js:190`, o form de edição do portal precisa mostrar as tags atuais da obra). Não estava na tabela de rotas da spec: foi acrescentada na Task 9 porque sem listagem a aba Obras não sobrevive a um reload. Os capítulos continuam vindo de `GET /api/content/series/:id/episodes`, que já é dono-aware
- `POST /series` (`portal.js:261`) — cria série **draft**. Allowlist `PORTAL_SERIES_FIELDS` = `title, description, cover_image, content_rating_sugerida, tags` (`portal.js:222` — `tags` entrou na Task 6 do Bloco 2, inversão deliberada do contrato do Bloco 1; `genre`/`content_type`/`isPublished`/`content_rating` é que ficam de fora de propósito, o Master preenche/define na aprovação); `content_type: 'hiqua'` e `isPublished: false` **pinados depois do spread** (`portal.js:292-293`), então body malicioso é ignorado. `channelId` só é exigido quando o dono tem >1 canal ativo
- `PUT /series/:id` (`portal.js:312`) — só em rascunho **NÃO submetido**; publicada ou já submetida → **403** ("peça o ajuste ao editor pela thread"). Mantém o pilar "nada muda no ar sem aprovação"
- `POST /series/:id/episodios` (`portal.js:351`) — capítulo novo, `status: 'draft'` forçado; allowlist `PORTAL_EPISODE_FIELDS` = `title, description, episode_number, thumbnail` (`portal.js:224`) — `status`/`bunnyVideoId`/campos de vídeo nunca entram. Vale para série draft **e** publicada (capítulo novo em obra no ar é o fluxo normal). **Fase 5 Bloco 2 (Task 8):** `episode_number` duplicado na MESMA série → 400 (`portal.js:369-371`), validação na rota, ANTES do `Episode.create` — mesma checagem do lado admin (`content.js`)
- `POST /episodios/:id/paineis` (`portal.js:402`) — reusa `services/episodePanelService.addPanels`, a MESMA função da rota admin (logo `translationLayers` funciona de graça). 403 se o episódio não é `draft` **ou** se já está submetido
- `POST /series/:id/enviar` (`portal.js:430`) — exige capa + ≥1 capítulo draft com painéis; marca `submittedAt`
- `POST /episodios/:id/enviar` (`portal.js:480`) — exige ≥1 painel e que a série já esteja publicada OU já submetida (enviar a série não envia os capítulos em cascata). **Materializa o fallback do thumbnail aqui**: sem thumbnail, vira o `image_url` do 1º painel (`portal.js:501-503`)
- **CastError → 404** (Fase 5 Bloco 2, Task 8, higiene do Bloco 1): as 5 rotas acima com `:id` (PUT `/series/:id`, POST `/series/:id/episodios`, POST `/episodios/:id/paineis`, POST `/series/:id/enviar`, POST `/episodios/:id/enviar`) tratam id malformado via `utils/routeErrors.js::responderCastError` (`portal.js:341`, `:392`, `:421`, `:460`, `:508`) — antes desta task, um id que não era um ObjectId válido caía no catch genérico e virava **500**
- `GET /mensagens` · `POST /mensagens` (`portal.js:562` e `:612`) — thread VIGENTE do dono (`ownerUserId = req.user.id`, `arquivadaEm: null`); histórico de antecessor nunca aparece. GET marca como lidas as do editor na thread inteira; paginação `limit` (padrão 100, teto 200) + `before` (ISO). No POST, só `texto` (e `canalId` condicional) entram no documento — `refTipo/refId/autorTipo/autorUserId/ownerUserId` do body são sempre ignorados

### `adminPortal.js` — Portal do Ilustrador, lado do EDITOR (Fase 5, Bloco 1)
Montado em `/api/admin` **puro** (`server.js:258`), não em `/api/admin/mensagens` — a spec pina os caminhos como filhos diretos de `/admin`. Toda rota é `verifyToken` + `requireAdmin`.
- `GET /mensagens/:canalId` (`adminPortal.js:78`) — todas as threads do canal agrupadas por (`ownerUserId`, `arquivadaEm`), **vigente primeiro** e arquivadas da mais recente para a mais antiga; o admin vê tudo. Abrir marca como lidas as do ilustrador no canal inteiro
- `POST /mensagens/:canalId` (`adminPortal.js:124`) — mensagem do editor. `ownerUserId` vem SEMPRE de `channel.ownerId` resolvido no servidor, nunca do body. `refTipo`/`refId` opcionais, validados como PAR e **contra o canal** por `validarRef` (`adminPortal.js:36`) — apontar a mensagem para a obra de outro canal é 400
- `GET /aprovacoes` (`adminPortal.js:181`) — fila de pendentes. Shape **flat** com `tipo: 'series'|'episode'`, ordenada por `submittedAt` ASC, sem N+1 (4 queries constantes). Cada card traz o preview que o Master usa para decidir: capa/thumbnail, descrição, `content_rating_sugerida`, `genre`/`tags` atuais, canal, e — no episódio — `panelCount` e o `isPublished` da série-mãe. Fase 5 Bloco 2 (Task 6): resposta também traz `naoClassificadas` (contagem de séries publicadas com `content_rating` null/ausente — badge do `AdminDashboard`)
- `POST /aprovacoes/series/:id/aprovar` (`adminPortal.js:263`) — `genre`/`tags`/`content_rating` são opcionais no body (o Master preenche na mesma ação), mas `content_rating` final é **obrigatório** para aprovar (Fase 5 Bloco 2, Task 6 — 400 "Classificação etária é obrigatória para aprovar", mensagem PINADA, checado ANTES de chamar `applySeriesUpdate`). Delega a `applySeriesUpdate` com `isPublished: true` + `submittedAt: null`: gênero final ausente → **400** sem meio-publicar; tags inválidas → 400. Registra `APROVAR_SERIE_PORTAL` no `AdminLog`. **Fase 5 Bloco 2 (Task 8, Dívida T6 (1)):** o catch mapeia CastError → 404 SÓ quando `err.path === '_id'` (`adminPortal.js:318`, via `utils/routeErrors.js::responderCastError`) — antes desta task, `content_rating` recebendo um array/objeto no body lançava CastError (não ValidationError) que caía no MESMO catch e virava 404 "Série não encontrada", mascarando um erro de input do Master; agora vira 400 legível
- `POST /aprovacoes/episodes/:id/aprovar` (`adminPortal.js:332`) — exige série já publicada (senão 400, "aprove a série primeiro"). Publica e dispara **inline** o mesmo par push + recálculo dos outros pontos de publicação de episódio (`adminPortal.js:352-355`) — é o 7º ponto, com as mesmas assinaturas dos 6 da Fase 4. Registra `APROVAR_EPISODIO_PORTAL`
- `POST /aprovacoes/:tipo/:id/devolver` (`adminPortal.js:380`) — genérica. Aceita `series`, `episode` e também o plural `episodes` (normalizado para o singular ANTES de qualquer gravação, `adminPortal.js:387` — simetria com a URL da rota de aprovar). Limpa `submittedAt` (a obra volta a ser editável/reenviável), cria a `MensagemPortal` do editor com `refTipo/refId` apontando o recurso, e registra `DEVOLVER_SERIE_PORTAL`/`DEVOLVER_EPISODIO_PORTAL`. **NÃO** toca `isPublished`/`status` e **não** cascateia para os capítulos da série
- **CastError → 404 restrito a `err.path === '_id'`** (Fase 5 Bloco 2, Task 8): as 5 rotas acima usam `utils/routeErrors.js::responderCastError` nos catches (era `if (err.name === 'CastError') → 404` incondicional em todas — o achado da Dívida T6 (1) foi generalizado para o arquivo inteiro, mesmo nas rotas onde hoje só `_id` é castável)

### `sinalizacao.js` — Sinalizar conteúdo, lado do LEITOR (Fase 5, Bloco 3 — Curadoria)
Montado em `/api/content` (`server.js:267`), **ao lado** de `content.js` no mesmo prefixo (as duas rotas novas não colidem com nenhum caminho existente). Regra 5 do Vin ("uma conta = uma sinalização por obra") e regra 8 ("ninguém vê quantas denúncias uma obra recebeu"): nenhuma resposta daqui devolve contagem nem existência de caso — só o estado do PRÓPRIO usuário.
- **Composição do 404** — `serieSinalizavel` (`sinalizacao.js:32-39`), usada pelas DUAS rotas: série inexistente, `isPublished:false` **ou** invisível pelo `serieVisivelPara` do filtro parental → 404 `'Série não encontrada.'`, sem nenhum write. O `isPublished` vale para **TODOS, inclusive admin e dono do canal** (`sinalizacao.js:34`) — diferente de `GET /content/series/:id`, que abre exceção para admin/dono via `podeVerRascunho` (`content.js:178-181`): rascunho e obra despublicada pela curadoria não podem acumular sinal nem confirmar existência
- `POST /series/:id/sinalizar` (`sinalizacao.js:41`) — `verifyToken` + `sinalizacaoLimiter` (ver `middlewares/CONTEXT.md`). **A validação de body vem ANTES do 404**: motivo fora do enum (`:44-46`), `descricao` não-string → 400 em vez de coagir com `String()` (`:50-52`), acima de `DESCRICAO_MAX` (`:54-56`), `outro` sem descrição (`:57-59`). Nenhuma delas depende da série, então a ordem não vaza existência
- **Dono do canal da obra → 400 `{ error, code: 'propria_obra' }`** (`sinalizacao.js:64-69`), sem gravar — o dono sinalizando a própria obra só poluiria o S
- **Idempotência**: `findOne` antes → 200 `{ jaSinalizada: true }` sem alterar nada (`sinalizacao.js:71-72`); `create` → 201 `{ jaSinalizada: false }` (`:102`); corrida de duplo clique (`E11000`) → o mesmo 200 (`:98`). O motivo NUNCA é trocado numa 2ª tentativa (não se "gamifica" a categoria), e o unique é **para sempre** — quem sinalizou no ciclo 1 não sinaliza a recaída no ciclo 2
- **A validade é decidida NA ESCRITA** (`sinalizacao.js:81-87`): motivo grave (`direitos_autorais`/`conteudo_proibido`) nasce `valida:true` sem exigir consumo (o titular de direitos reconhece a cópia pela capa); motivo normal exige consumo REAL — `EngagementEvent.exists({seriesId, userId, type: view/read, flagged:false})` (`:85`) —, senão grava `valida:false, invalidaMotivo:'sem_consumo'` (`:86`). `ReadingProgress` NÃO conta (`PUT /me/progress` aceita ids arbitrários sem barreira). **A resposta ao leitor é idêntica nos dois casos** — não se ensina a brigada qual caminho "conta"
- Snapshot `contaCriadaEm = usuario.createdAt || usuario._id.getTimestamp()` (`sinalizacao.js:93` — contas anteriores ao `timestamps` do schema de `User` não têm `createdAt`) e `ipHash = pseudonymize(req.ip)` (`:94`, o MESMO pseudonimizador do `engagementLogger`, `sinalizacao.js:24`). A idade mínima da conta é aplicada só **na avaliação**, nunca aqui
- **O gatilho só é disparado se a sinalização for válida** (`sinalizacao.js:103-115`): fire-and-forget, DEPOIS do `res.status(201)` (`:102`) e dentro de um `try/catch` que protege só a chamada síncrona (a rejeição da promise já é absorvida dentro de `curadoriaService.dispararAvaliacao`). Falha na avaliação nunca muda a resposta do leitor
- `GET /series/:id/sinalizacao` (`sinalizacao.js:127-138`) — `verifyToken`; devolve `{ jaSinalizada, motivo }` **do próprio usuário** e nada mais, com a MESMA composição de 404 (`:129`)
- Conta `isActive:false` recebe 403 **herdado** do middleware global de `/api` (`server.js:234-247`) — não há uma linha sequer de conta desativada nesta rota

### `adminCuradoria.js` — Fila de Revisão, lado do CURADOR (Fase 5, Bloco 3 — Curadoria)
Montado em `/api/admin` **puro** (`server.js:262`), ao lado de `adminPortal.js` — mesma decisão de caminho do Bloco 1. `router.use(verifyToken, requireAdmin)` no topo (`adminCuradoria.js:26`), então não-admin não alcança nenhuma rota. Regra 1 do Vin: **nada aqui é automático** — as 4 decisões são humanas, e "remover" é despublicar, nunca `DELETE`.
- `GET /curadoria?status=` (`adminCuradoria.js:44`)
  - **Reavaliação de maturação ANTES de listar, em `try/catch` próprio** (`:53-57`): o Master abrir a fila reconta as sinalizações de contas que completaram a idade mínima (`svc.reavaliarPendentes()`, `:54`); um erro ali é logado e a listagem segue — a fila não pode cair por causa do gatilho
  - **O histórico filtra por `status: 'fechado'`, NÃO por `emAberto:false`** (`:65`): um caso que ficasse meio-fechado por falha parcial nunca se disfarça de decidido. A fila é `emAberto:true`, `abertoEm` asc, teto 500; o histórico é `decisaoEm` desc, teto 100 (`:66-67`)
  - **Ordenação** (`ordenar`, `:28-41`): `prioridade:'grave'` primeiro; entre graves, `S_grave` desc (`:33`); entre normais, `S/limiar` desc (`:35-37`); empate resolvido por `abertoEm` asc (`:39`)
  - Item: a contagem VIVA (`S`, `S_grave`, `semConsumo`, `contasRecentes`, `ipsDistintos`) só existe nos abertos (`:96-97`) — no histórico vai o snapshot do gatilho com esses campos zerados (`:93`), e o painel esconde a linha por isso. `descricoes` são do CICLO (`revisadaEm:null`) e saem sem `userId` (`:81-87`). `thread` = últimas 10 mensagens da thread VIGENTE do canal, **sem filtro de `refId`** (a resposta do artista nasce com `refId` null) e **somente leitura** — não marca `lidaEm`, ao contrário de `GET /admin/mensagens/:canalId` (`:102-104`). Resposta `{ casos, total, graves }` (`:121`)
- **As 4 ações têm o mesmo esqueleto**: `carregarCasoAberto` → `reivindicar` → efeito → `fecharCaso` → `AdminLog`, com um `catch` que **devolve a reivindicação** e relança (`:223`, `:258`, `:320`, `:362`)
  - `carregarCasoAberto` (`:129-134`): caso inexistente → 404 (`:131`); caso já fechado → **409 `'Caso já fechado.'`** (`:132`)
  - `reivindicar` (`:172-176`) chama o mutex do serviço e devolve o TOKEN de posse; sem token → **409 `'Este caso está sendo decidido por outro curador. Recarregue a fila.'`** (`EM_DISPUTA`, `:171`). **Os dois 409 são situações diferentes para quem está na tela**: o primeiro é decisão já tomada (recarregar mostra o caso no histórico); o segundo é outro curador com a ação em curso AGORA — o caso pode voltar à fila se ele falhar
  - `POST /curadoria/:casoId/aprovar` (`:203-227`) — não altera a obra, mas **reivindica assim mesmo** (`:213`): sem passar pelo mutex, ela venceria o fechamento de um `remover` que JÁ despublicou a obra, e o artista receberia "obra mantida sem alterações" com a obra fora do ar. `abuso:true` (`:216`) marca as válidas do ciclo como abusivas dentro de `fecharCaso`
  - `POST /curadoria/:casoId/reclassificar` (`:229-269`) — `content_rating` fora de `kids|teen|young` → 400 antes de qualquer escrita (`:232-234`); reivindica (`:242`), **reconfere a posse imediatamente antes de tocar na obra** (`aindaEhMeu`, `:187-189` e `:246`), `applySeriesUpdate` (`:250`), fecha (`:252`), avisa (`:254`), loga (`:255`)
  - `POST /curadoria/:casoId/solicitar-correcao` (`:271-324`) — a única ação que **não fecha** o caso: `texto` 1–1500 (`:273`, `textoAdmin` em `:136-141`), obra sem canal → 400 conferido ANTES de reivindicar (`:283-288`), update CONDICIONAL a `emAberto:true` + token, que já libera o mutex no próprio `$set` (`:303-306`), `matchedCount === 0` → devolve e 409 (`:307-310`), falha REAL no envio da mensagem → **500** (`:314-316`), não 400 "sem canal" — são coisas diferentes (entrada inválida do curador × falha nossa)
  - `POST /curadoria/:casoId/remover` (`:326-376`) — `motivo` obrigatório (`:328`), reivindica (`:339`), reconfere a posse (`:343`), **despublica** com `{ isPublished:false, submittedAt:null }` (`:354`; sem limpar `submittedAt`, obra publicada pelo PUT genérico do admin voltaria na hora à Fila de Aprovação e o artista não conseguiria editar), fecha (`:356`), avisa (`:358`), loga (`:359`). O `logger.error` do catch só afirma "APÓS alterar a obra" quando a obra foi mesmo alterada (`:370-372`)
  - `observacao` é OPCIONAL e INTERNA (nunca vai ao artista): não-string ou > 2000 → 400 explícito, nunca truncamento silencioso (`observacaoDe`, `:149-155`)
- `tratarErro` (`:195-201`): `CastError` do `:casoId` → 404 (`utils/routeErrors.js`); `err.status` — o 409 que `fecharCaso` lança — → o status próprio (`:197`); `ValidationError` → 400; o resto → 500

### Fase 5 Bloco 3 — o que a curadoria mudou em rotas que já existiam
- **`adminPortal.js` · `GET /aprovacoes`** — ganhou `curadoria: { abertos, graves }` (badge da sidebar) e, por item de série, `removidaPelaCuradoria` (`adminPortal.js:262`, resposta em `:283`). As DUAS leituras ficam **fora** do `Promise.all` original e num `try/catch` próprio que degrada para badge zerado e nenhum aviso (`adminPortal.js:195-220`): esta rota é código do Bloco 1 **em produção** e o único caminho do Master para publicar — não pode cair junto com um bloco novo. `removidaPelaCuradoria` é o ÚLTIMO caso `decisao:'remover'` da série, por uma query `$in` ordenada por `decisaoEm` desc + `Map` (`:212-215`, `:221-225`), sem N+1
- **`content.js` · `DELETE /series/:id`** — o `Promise.all` da limpeza de órfãos passou a apagar `Sinalizacao` e `CasoCuradoria` da obra (`content.js:277-278`); a série continua sendo apagada **por último** (`:280`), para o DELETE poder ser repetido se alguma limpeza falhar. Sem isso, o caso órfão ficaria eterno na fila (com o badge preso) e a sinalização vazaria no export do leitor apontando para série inexistente — o mesmo bug que o comentário `content.js:259-264` já descrevia para votos/favoritos. A `MensagemPortal` do aviso **não** é apagada de propósito: é comunicação privada do artista, e o `refId` fica órfão como já acontece com a devolução da Fila de Aprovação
- **`account.js` · `GET /me/export`** — ganhou `sinalizacoes`, só as do TITULAR como leitor (o caso é da OBRA e não entra em export nenhum — nem no do artista). São **duas queries**: `find` sem `populate` (`account.js:122`) + `Series.find` com `Map` de títulos (`:125-127`) — `populate` de série apagada devolveria `seriesId: null` e o export perderia o id. Allowlist campo a campo (`:210-216`): `seriesId`, `titulo` (null se a série foi apagada), `motivo`, `descricao`, `createdAt` — sem `valida`/`invalidaMotivo`/`ipHash`/`contaCriadaEm`/`revisadaEm`/`_id`
- **`account.js` · `DELETE /me`** — `Sinalizacao.deleteMany({ userId })` entra no mesmo `Promise.all` da exclusão (`account.js:361`); a descrição vai junto e o S da obra recalcula na próxima avaliação
- **`account.js` · primeiro admin** — o critério "admin/superadmin de `createdAt` mais antigo", que era um `findOne` inline nesta rota, virou `utils/primeiroAdmin.js` e a rota passou a chamá-lo (`account.js:38` e `:305`): a curadoria precisa do MESMO usuário como autor do aviso automático ao artista

### `parental.js` — Classificação etária e Preferências de conteúdo (Fase 5, Bloco 2)
Montado em `/api/parental` (`server.js:270`). `router.use(verifyToken)` no topo (`parental.js:33`) — não há leitura/escrita anônima. `pinHash` NUNCA sai em nenhum shape: toda resposta é montada campo a campo (`shapeParental`, `parental.js:35-41`), nunca por spread do documento do Mongoose.
- `GET /` (`parental.js:45`) — `classificacaoEtaria` (default `young`), `tagsBloqueadas`, `temPin` (derivado de `parental.pinHash` via `.select('+parental.pinHash')`) e `vocabulario` (array de `{slug, rotuloPt}` do vocabulário oficial — `parental.js:52`, `VOCABULARIO.map(...)` de `utils/tagsVocabulario.js` — para o frontend nunca hardcodar; canal PINADO pela spec: os toggles do leitor usam ESTE `vocabulario`, nunca um import direto do JSON, que é o canal dos chips do admin/portal)
- `PUT /` (`parental.js:63`) — `classificacaoEtaria?`/`tagsBloqueadas?`. Allowlist ESTRITA (só essas duas chaves são lidas do body — `parental.js:82-108`). Com PIN definido (`temPin`), QUALQUER mudança exige `pin` correto no body (`avaliarTentativaPin`, `parental.js:71-80`) — INCLUSIVE as próprias `tagsBloqueadas` do adulto (decisão consciente da spec: sem isso a criança desbloqueia sozinha). `tagsBloqueadas` é deduplicado antes de gravar (`parental.js:107` — `[...new Set(tags)]`, evita um `$nin` gigante). Slug fora do vocabulário → 400
- `POST /pin` (`parental.js:129`) — define (sem PIN prévio, só `novoPin`) / troca (`pinAtual` + `novoPin`) / remove (`pinAtual` + `remover:true`). PIN de 4-6 dígitos numéricos (`formatoPinValido`, `parental.js:122-124`), `bcrypt.hash(novoPin, 12)` — mesmo custo de senha (`server.js`). Troca/remoção reusam `avaliarTentativaPin` (mesmo rate limit persistido do `PUT /`)
- `POST /pin/recuperar` (`parental.js:183`, `accountLimiter`) — inicia a recuperação via token por e-mail (`ParentalPinResetToken`, TTL 1h no model + checagem explícita `parental.js:31`/`:226`, redundante contra a varredura não-instantânea do TTL do Mongo). Conta LOCAL exige `password` no body (401 sem/errada — mesma prova de identidade da exclusão de conta); conta SOCIAL não checa senha (a rota já é autenticada — `verifyToken` — e não há a neutralidade do forgot-password público a preservar aqui, que é anônimo por natureza)
- `POST /pin/recuperar/confirmar` (`parental.js:216`, `accountLimiter`) — REMOVE o PIN (usuário define um novo depois, já sem bloqueio) e zera tentativas/bloqueio. Token escopado ao usuário LOGADO (`ParentalPinResetToken.findOne({token, userId: req.user.id})`) — diferente do reset de senha, nunca público. Todos os tokens pendentes do usuário caem junto na confirmação (mesmo padrão do reset de senha) — barra reuso
- `DELETE /api/account/me` (rota vive em `account.js`, não aqui) também exige o PIN quando `temPin` — ver `account.js` acima, ordem senha→PIN→409 de canal
- Rate limit do PIN é PERSISTIDO no próprio `User` (`parental.pinTentativas`/`parental.pinBloqueadoAte`, nunca em memória — sobrevive a restart do processo; key = userId, a rota já exige a própria sessão), fórmula e comparação centralizadas em `services/parentalPinService.js` (ver `services/CONTEXT.md`)

### `donation.js` — Doações
- `POST /donate` — Processa uma doação

### `mobilePayment.js` — Pagamento Mobile
- Integração com processador de pagamento alternativo para dispositivos móveis

### `favorites.js` — Meus Favoritos
Montado em `/api/favorites` (todas com `verifyToken`).
- `GET /` — lista favoritos da conta (filtra séries despublicadas/deletadas, critério `isPublished === true`)
- `POST /:seriesId` — adiciona (upsert idempotente; corrida E11000 tratada como sucesso). Fase 4, Bloco 4: dispara `recommendationService.dispararRecalculo(seriesId, 'favorito')` SEMPRE (na resposta de sucesso E no `E11000`) — a rota é idempotente e sempre responde `favorited: true` sem saber se criou ou só confirmou um favorito já existente
- `DELETE /:seriesId` — remove

### `account.js` — Conta e LGPD
Montado em `/api/account`.
- `PUT /me/consent` — consentimento de marketing
- `GET /me/export` — export de dados (LGPD; inclui `readingProgress` com `percent` e `position`). Fase 4 Bloco 3: inclui `superReaderContributions` (`seriesTitle`, `amountCents`, `currency`, `createdAt` — sem `stripeSessionId` nem os campos de share, que são detalhe contábil do repasse ao autor, não dado sobre o titular). Fase 5 Bloco 1: inclui `portalMessages` (query `account.js:107`, resposta `account.js:178-186`) — mensagens do portal em que o titular é autor **OU** dono da thread, **inclusive de threads já arquivadas** (ele era o dono vigente quando escreveu/recebeu; a troca de dono não revoga o direito de acesso aos próprios dados). Sem `autorUserId`/`ownerUserId` crus: `autorTipo` já diz quem escreveu, sem expor o id de terceiros. `channels` ganhou `isActive` (`account.js:143`) — canal desativado ainda é vínculo do titular
- `DELETE /me` — exclusão de conta com limpeza de engajamento (inclui `ReadingProgress`). Fase 4 Bloco 3: `SuperReaderContribution` NÃO é apagada — é **anonimizada** (`updateMany({userId}, {$set: {userId: null}})`), pois o valor repassado ao autor é registro contábil do relatório de royalties (soma por canal/período, não por usuário). Ordem das checagens (`account.js:214-262` — todas ANTES de qualquer efeito colateral, Stripe/deletes incluídos): senha (só conta local) → **PIN de proteção, se `temPin`** (Fase 5 Bloco 2, `account.js:229-245` — `avaliarTentativaPin`, MESMO rate limit persistido das rotas de `/api/parental`; de QUALQUER provider, inclusive social, que não passa pela checagem de senha) → 409 se dono de canal ATIVO (`account.js:257-262`). Fase 5 Bloco 1 (Task 8), três mudanças:
  1. **409 se o titular é dono de canal ATIVO** (`account.js:257-262`), checado **antes de qualquer efeito colateral** — inclusive antes do cancel da assinatura no Stripe, para que o request bloqueado seja atômico. A porta de saída existe e é do editor: `POST /api/channels/:id/desativar` **ou** transferir a titularidade (`PUT /api/channels/:id` com `ownerEmail`) — qualquer um dos dois desbloqueia. Bloqueio sem porta de saída seria negativa permanente do Art. 18 VI
  2. **`Channel.deleteMany({ownerId})` foi REMOVIDO.** Canais **inativos** do titular são **transferidos ao admin mais antigo** (`role` admin/superadmin, menor `createdAt`), nunca apagados: obra publicada jamais some com a conta do ex-dono. O lookup do admin roda **antes** do bloco do Stripe, para o abort de 500 (nenhum admin no sistema) não acontecer com a assinatura já cancelada
  3. `MensagemPortal.deleteMany({ autorUserId })` — só as mensagens **autoradas** pelo titular (comunicação privada dele). As do editor na mesma thread ficam
- `POST /me/avatar` — upload de foto de perfil (multer memória → sharp 512×512 webp → Bunny Storage `lorflux/avatars/`)

### `progress.js` — Progresso de Leitura e "Continuar" (Fase 4)
Montado em `/api/me`. Todas as rotas aceitam conta OU visitante (`optionalAuth` + `utils/requestIdentity.js`).
- `PUT /progress` — grava (upsert) onde o usuário parou. Fase 4, Bloco 4: `services/progressService.dispararSeConcluido` dispara `recommendationService.dispararRecalculo(seriesId, 'progresso_concluido')` quando o documento SALVO tem `completed: true` (não todo save — a maioria tem `completed: false`); releitura de quem já concluiu pode re-disparar, aceito e idempotente
- `GET /progress/:episodeId` — progresso de UM episódio específico, sem as regras de poda/dedupe/teto do carrossel — devolve a linha crua ou `null`. Usado pela restauração de "onde parei" no `WebtoonReader`/`VerticalPlayer` (a lista de `/continue` é lossy demais para essa pergunta)
- `GET /continue` — carrossel "Continuar"; aceita `?contentType=hqcine|vcine|hiqua` para filtrar e aplicar o teto de 20 obras só dentro daquele tipo
- `POST /progress/claim` — migra o histórico do visitante (`anonymousId`) para a conta no login/cadastro

### `push.js` — Notificações Push (Fase 4, Bloco 2)
Um único router montado uma vez em `/api` — mistura rota pública com rotas autenticadas.
- `GET /push/public-key` — SEM auth; devolve `{ publicKey }` (pode vir `null` se VAPID não estiver configurado em produção — o front trata)
- `POST /me/push/subscribe` — `verifyToken`; upsert por `endpoint` (`{ endpoint, keys: { p256dh, auth } }` no body); o aparelho passa a pertencer a quem está logado nele agora (takeover de dono é intencional); devolve `{ subscribed: true }` (200 se já existia, 201 se novo)
- `DELETE /me/push/subscribe` — `verifyToken`; remove só o endpoint do próprio usuário (`{ endpoint }` no body); devolve `{ removed: <deletedCount 0 ou 1> }`
- `GET /me/push/status` — `verifyToken`; `?endpoint=` opcional; devolve `{ thisDevice, anyDevice }` (`thisDevice` = esse endpoint pertence ao logado; `anyDevice` = o logado tem alguma inscrição, em qualquer aparelho)

### `settings.js` — Configurações
- `GET /public` — settings públicas (tagline, anúncios, `google_client_id` vindo do env quando configurado)
- `GET /`, `PUT /:key` — CRUD (admin)

### `royalties.js` — Motor de Royalties (Fase 3)
Montado em `/api/admin/royalties` (tudo `verifyToken` + `requireAdmin`).
- `GET /report?period=YYYY-MM` — pontos válidos por canal (view/read não-flagged), share, pool sugerido (impressões÷1000×`premium_cpm_rate` + premium ativos×`royalty_premium_per_sub`), alertas de anomalia. Fase 4 Bloco 3: resposta ganha `superReader: { porCanal: [{channelId, channelName, apoios, autorCents}], totalAutorCents, totalPlataformaCents, totalApoios }` — soma de `SuperReaderContribution` (`authorShareCents`/`platformShareCents`) por canal no período, **fora** de `poolSuggested` e do `breakdown` (apoio direto ao autor, não entra no pool mensal)
- `POST /close` — fecha o período com `poolFinal` confirmado (snapshot em `RoyaltyPeriod`); Super Reader não participa (sem alteração nesta rota)
- `GET /periods` — períodos fechados
- `GET /verify-integrity` — re-percorre a cadeia de hash do log de eventos
- `GET /export.csv?period=YYYY-MM` — CSV do relatório. Fase 4 Bloco 3: bloco separado ao final do arquivo (linha em branco + cabeçalho "Super Reader (direto ao autor)" + `canal;apoios;autor;plataforma`, valores em decimal `.toFixed(2)`) — não altera nenhuma linha/coluna existente do CSV do pool

---

## Padrões

- Todas as rotas são montadas no `server.js` sob o prefixo `/api`
- Rotas protegidas usam os middlewares `verifyToken`, `requireAdmin` ou `requirePremium`
- Arquivos `.js` (CommonJS) — o backend não usa TypeScript
- Validação de dados via `validators/contentValidator.js` (Joi)
- **Rotas de autenticação vivem no `server.js`** (não nesta pasta): register, login, `POST /api/auth/google` (Google Identity Services — verifica ID token, vincula por e-mail verificado, dormente sem `GOOGLE_CLIENT_ID`), refresh-token, logout, forgot/reset-password, `/auth/me`
- **Ownership: 403 vs 404.** Padrão adotado na Fase 5, Bloco 1 e válido para tudo que vier depois: **403** = "essa área não existe para você" (não é ilustrador, não é admin) — não há segredo a esconder; **404** = "esse recurso específico não é seu" (série/capítulo/canal de outro dono, ou inexistente) — 403 aqui confirmaria a existência do rascunho alheio. Ver `routes/portal.js:36` (403) contra `portal.js:232`/`:245` (404)

---

## Dívidas conhecidas (Fase 5, Bloco 1 — triadas na revisão final, não bloqueantes)

- **`followers[]` cru fora do `GET /:id`**: `GET /api/channels/me`, a resposta do `PUT /api/channels/:id` e a de `POST /:id/desativar` devolvem o documento cru, com o array de userIds dos seguidores. Só o dono do canal e o admin alcançam essas três respostas, e o shape público já foi corrigido — mas o mesmo argumento de dado pessoal vale aqui. Fix pequeno (projeção/`select` nas três) para um bloco futuro
- ~~CastError → 500 em id malformado~~ — **FECHADA na Fase 5 Bloco 2, Task 8** (higiene do Bloco 1, em dois commits — a 1ª rodada fechou canais + `PUT /portal/series/:id` + `POST /portal/episodios/:id/paineis` + as demais rotas de portal com `:id`; um fix round posterior fechou o que sobrava em `content.js` — `GET /series/:id`, `GET /series/:id/episodes`, `GET /episodes/:id`, achado pelo revisor porque a rodada 1 não tinha inventariado este arquivo): todas tratam via `utils/routeErrors.js::responderCastError` — 404 quando `err.path === '_id'` (ver seções `channels.js`/`portal.js`/`adminPortal.js`/`content.js` acima para o detalhe de cada rota, inclusive a mecânica DIFERENTE do `ValidationError` em `channels.js`)
- **`podeVerRascunho` não checa `isActive`**: o dono de um canal DESATIVADO continua enxergando os próprios rascunhos pelas rotas de conteúdo (`GET /content/series/:id` etc.), embora o portal inteiro já lhe responda 403. É consistente com "não apagar obra de ninguém", mas é uma decisão implícita
- ~~Sem rota de reativar canal~~ — **FECHADA na Fase 5 Bloco 2, Task 8**: `GET /channels?includeInactive=true` + `isActive` no `select` + `POST /:id/reativar` (ver seção `channels.js` acima). Reativar NÃO desarquiva `MensagemPortal` — decisão documentada no próprio código
- **Item submetido de canal desativado continua aprovável**: a fila (`adminPortal.js:181`) não filtra por `isActive`, então o Master pode publicar a obra de um ilustrador já revogado. É defensável (a decisão é do Master), mas é comportamento não especificado
- **`cover_image` e `panels[].image_url` aceitam qualquer string**: os formulários do portal são upload-only (nenhum campo de URL), mas um request forjado pelo próprio dono grava URL externa. Não é XSS (as duas viram `src` de `<img>`); o risco real é hotlink/pixel de rastreamento em conteúdo publicado. Validar host contra o CDN do Bunny é candidato natural do Bloco 2/5.1
- **`PUT /api/content/episodes/:id` aceita `episode_number` já usado por outro episódio da mesma série** (registrada no fix round da Fase 5 Bloco 2 Task 8, ao lado da validação nova de duplicidade): a checagem de `episode_number` duplicado só existe nos DOIS POSTs de criação (`POST /api/content/episodes` e `POST /api/portal/series/:id/episodios`, ver acima) — `PUT /api/content/episodes/:id` (`content.js:531`, `EPISODE_FIELDS` inclui `episode_number`) faz `findByIdAndUpdate` sem nenhuma checagem de colisão. Triagem para bloco futuro
